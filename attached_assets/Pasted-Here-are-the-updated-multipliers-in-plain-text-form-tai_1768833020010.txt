Here are the updated multipliers in plain text form, tailored exactly to your specified prediction selection options for each emoji in the Telegram animated dice game (based on current mechanics as of January 2026). All outcomes remain uniformly random with equal probability per possible value.

The fair multiplier = 1 / win probability (includes stake returned, e.g. 2.5× means bet 1 → get 2.5 back on win).  
The 0.5% house edge multiplier ≈ fair × 0.995 (rounded practically for bot use).

### 🏀 Basketball (values 1–5, each 20% probability)
- Predict "score" (values 4 or 5): win probability 40%, fair multiplier 2.50×, with 0.5% house edge ≈ 2.4875× (or rounded to 2.49×)
- Predict "miss" (values 1, 2, or 3): win probability 60%, fair multiplier 1.6667×, with 0.5% house edge ≈ 1.6583× (or rounded to 1.66×)
- Predict "stuck" (no such distinct outcome exists — all animations resolve to a score or miss based on the value; no separate "stuck" animation or value is documented): this option is not possible, so no multiplier applies

### ⚽️ Soccer/Football (values 1–5, each 20% probability)
- Predict "goal" (values 4 or 5): win probability 40%, fair multiplier 2.50×, with 0.5% house edge ≈ 2.4875× (or rounded to 2.49×)
- Predict "miss" (values 1, 2, or 3): win probability 60%, fair multiplier 1.6667×, with 0.5% house edge ≈ 1.6583× (or rounded to 1.66×)
- Predict "bar" (no such distinct outcome exists — the animation shows a goal scored or missed; no "bar" or post/crossbar variation is a separate result in the API or visuals): this option is not possible, so no multiplier applies

### 🎯 Darts (values 1–6, each ≈16.67% probability)
Use simple 1–6 values for predictions (no special named outcomes like bullseye/miss required unless you define them):
- Predict any exact value (specific 1, 2, 3, 4, 5, or 6): win probability ≈16.67%, fair multiplier 6.00×, with 0.5% house edge ≈ 5.97×

### 🎳 Bowling (values 1–6, each ≈16.67% probability)
Use simple 1–6 values for predictions:
- Predict any exact value (specific 1, 2, 3, 4, 5, or 6): win probability ≈16.67%, fair multiplier 6.00×, with 0.5% house edge ≈ 5.97×

### 🎲 Dice (values 1–6, each ≈16.67% probability)
Use simple 1–6 values for predictions:
- Predict any exact value (specific 1, 2, 3, 4, 5, or 6): win probability ≈16.67%, fair multiplier 6.00×, with 0.5% house edge ≈ 5.97×

Notes:
- "Stuck" for basketball and "bar" for soccer are not real separate outcomes in Telegram's system — the animations always end in a clear miss (low values) or success (high values), with no stuck/bar/post documented or observed in recent sources.
- For the 40%/60% bets (score/goal vs miss), the 0.5% edge versions (especially rounded like 2.49× and 1.66×) are very player-friendly and recommended.
- If you want to force "stuck" or "bar" as custom fun options, you could treat them as 0% win probability (instant loss), but that's not based on actual mechanics.

Let me know if you want to add more custom rules! 🏀⚽️🎯